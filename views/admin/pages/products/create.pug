extends ../../layouts/default.pug

block main 
    h1(class="mb-4") Thêm mới sản phẩm 

    form(action=`${prefixAdmin}/products/create` id="form-create-product" method="POST")
        div(class="form-group")
            lable(for="title") Tiêu đề
            input(type="text" class="form-control" id="title" name="title" required)
        div(class="form-group")
            lable(for="desc") Mô tả
            textarea(type="text" class="form-control" id="desc" name="description" rows="5" required)
        div(class="form-group")
            lable(for="price") Giá
            input(type="number" class="form-control" id="price" name="price" value="0" min="0")
        div(class="form-group")
            lable(for="discount") % Giảm giá
            input(type="number" class="form-control" id="discount" name="discountPercentage" value="0" min="0")
        div(class="form-group")
            lable(for="stock") Số lượng
            input(type="number" class="form-control" id="stock" name="stock" value="0" min="0")
        div(class="form-group")
            lable(for="image") Ảnh
            input(type="text" class="form-control" id="image" name="image")
        div(class="form-group")
            lable(for="position") Vị trí
            input(type="text" class="form-control" placeholder="Tự động tăng dần" id="position" name="position" min="1")
        <div class="form-group form-check form-check-inline">
            <input class="form-check-input" type="radio" name="status" id="statusActive" value="active" checked>
            <label class="form-check-label" for="statusActive">
                Default Hoạt động
            </label>
        </div>
        <div class="form-group form-check form-check-inline">
            <input class="form-check-input" type="radio" name="status" id="statusInactive" value="inactive">
            <label class="form-check-label" for="statusInactive">
                Default Không hoạt động
            </label>
        </div>
        div(class="form-group")
            button(type="submit" class="btn btn-primary") Tạo mới


    script(src="/admin/js/products.js")